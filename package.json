{
  "name": "stages-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "dotenv": "^16.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "build:dev": "react-scripts build",
    "build:stag": "cross-env REACT_APP_NODE_ENV=staging react-scripts build",
    "build:prod": "cross-env REACT_APP_NODE_ENV=production react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:dev": "cross-env REACT_APP_NODE_ENV=development HOST=localhost PORT=3001 react-scripts start",
    "start:stag": "cross-env REACT_APP_NODE_ENV=staging HOST=localhost PORT=3002 react-scripts start",
    "start:prod": "cross-env REACT_APP_NODE_ENV=production HOST=localhost PORT=3003 react-scripts start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
